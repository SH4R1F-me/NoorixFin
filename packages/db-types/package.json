{
  "name": "@noorixfin/db-types",
  "version": "0.1.0",
  "private": true,
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "generate": "supabase gen types typescript --local --schema public > src/database.types.ts",
    "check:fresh": "node scripts/check-fresh.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.0"
  }
}
